package main.connection;

import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.SQLException;

public class DBproper {
	private static final String DATABASE_DRIVER = "com.mysql.jdbc.Driver";
	private static final String DB_URL = "jdbc:mysql://localhost:3306/userDB?**rewriteBatchedStatements=true**";
	private static String USERNAME = "root";
	private static String PASSWORD = "1111";
	private static int Number = 10;

	// init connection object
	private Connection connection;
	// init properties object
	private DBproper properties;

	// create properties
	private DBproper getProperties() {
		if (properties == null) {
			properties = new DBproper();
			properties.setProperty("user", USERNAME);
			properties.setProperty("password", PASSWORD);

		}
		return properties;
	}

	// connect database
	public Connection connect() {
		if (connection == null) {
			try {
				Class.forName(DATABASE_DRIVER);
				connection = DriverManager.getConnection(DB_URL);
			} catch (ClassNotFoundException | SQLException e) {
				e.printStackTrace();
			}
		}
		return connection;
	}

	public Connection getConnection() {
		return connection;
	}

	public void setConnection(Connection connection) {
		this.connection = connection;
	}

	public static String getDatabaseDriver() {
		return DATABASE_DRIVER;
	}

	public void setProperties(DBproper properties) {
		this.properties = properties;
	}

	public static int getNumber() {
		return Number;
	}

	public static void setNumber(int number) {
		Number = number;
	}

	// disconnect database
	public void disconnect() {
		if (connection != null) {
			try {
				connection.close();
				connection = null;
			} catch (SQLException e) {
				e.printStackTrace();
			}
		}

	}
	
}
